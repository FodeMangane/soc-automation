Connecter Parse_Alert à TheHive . 
Configuration de TheHive_Create_Case
Cliquer sur l'action TheHive pour ouvrir ses paramètres.
Sélection de l'action : Choisir Create case dans la liste déroulante.
Configuration de base :
•	Name : TheHive_Create_Case
•	URL : http://10.0.30.104:9003
•	API Key : Votre Clé API
Paramètres du cas à créer
Title :
SSH Brute Force - $Parse_Alert.message.source_ip attacking $Parse_Alert.message.target_agent
Description :
SSH Brute Force Attack Detected

Alert Details:
Rule ID: $Parse_Alert.message.rule_id
Rule: $Parse_Alert.message.rule_description
Severity Level: $Parse_Alert.message.rule_level
Timestamp: $Parse_Alert.message.timestamp

Attack Information:
Source IP: $Parse_Alert.message.source_ip
Target Agent: $Parse_Alert.message.target_agent
Target IP: $Parse_Alert.message.target_ip
Attempted User: $Parse_Alert.message.attempted_user

Actions Taken:
IP automatically blocked by Wazuh Active Response
Case created in TheHive
IoC added to MISP
Slack notification sent
Summary :
SSH Brute Force from $Parse_Alert.message.source_ip targeting $Parse_Alert.message.target_agent (user: $Parse_Alert.message.attempted_user)
Autres paramètres :
Severity : 3 (HIGH) 
TLP : 2 (AMBER) 
PAP : 2 (AMBER) 
Tags : wazuh, ssh-brute-force, auto-response, $Parse_Alert.message.source_ip
Les variables $Parse_Alert.message.* seront automatiquement remplacées par les valeurs réelles lors de l'exécution.
